-r requirements.txt

# Optional only: built-in TTS, alignment, hosted image/video and parallax adapters.
# The filesystem workflow in AGENTS.md does not require these packages.
edge-tts==7.2.8
faster-whisper==1.1.0
fal-client==0.5.9
rembg==2.0.76
onnxruntime==1.23.2
scipy==1.18.0
scikit-image==0.26.0
opencv-python-headless
pooch
tqdm

