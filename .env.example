# No key is required for the manual/agent workflow.
# Optional provider adapters in pipeline/ can use owner-supplied keys:
ELEVENLABS_API_KEY=
FAL_KEY=
GEMINI_API_KEY=
WAVESPEED_API_KEY=

# Optional Vertex AI mode (uses gcloud auth instead of GEMINI_API_KEY):
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_ACCOUNT=
